<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Theme.UPTMTransportation" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/uptm_red</item>
        <item name="colorPrimaryVariant">@color/uptm_blue</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <!-- Splash theme (optional, untuk splash screen) -->
    <style name="Theme.UPTMTransportation.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_gradient</item>
        <item name="android:statusBarColor">@color/uptm_red</item>
        <item name="android:navigationBarColor">@color/uptm_red</item>
    </style>

    <style name="TextAppearance.Toolbar.Title" parent="TextAppearance.MaterialComponents.Headline6">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>
</resources>